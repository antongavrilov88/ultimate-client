import { COLORS } from './colors';
import { FONT_COLORS } from './fontColors';
import { PADDINGS } from './paddings';
import { SIZES } from './sizes';
import { BORDER_RADIUSES } from './borderRadiuses';
import { FONTS, FONT_WEIGHTS, FONT_SIZES, FONT_HEIGHTS, LINE_HEIGHTS } from './typography';
import { SPACES } from './spaces';

export {
  COLORS,
  FONT_COLORS,
  PADDINGS,
  SIZES,
  BORDER_RADIUSES,
  FONT_SIZES,
  FONT_WEIGHTS,
  FONT_HEIGHTS,
  LINE_HEIGHTS,
  FONTS,
  SPACES,
};
