enum SIZEABLE {
  LARGE = 'LARGE',
  MEDIUM = 'MEDIUM',
  SMALL = 'SMALL',
}

enum UI_THEMES {
  LIGHT = 'light',
  DARK = 'dark',
}

enum LOCAL_STORAGE_KEYS {
  TOKEN = 'token',
  THEME = 'theme',
  EMAIL = 'email',
}

enum API_TYPES {
  AUTH = 'auth',
  USERS = 'users',
}

enum INPUT_TYPES {
  PASSWORD = 'password',
  TEXT = 'text',
}

enum USER_DATA_FIELDS {
  EMAIL = 'email',
}

enum SIDE_MENU_ITEMS {
  MAIN = 'MAIN',
  COMPONENTS = 'COMPONENTS',
}

export {
  SIZEABLE,
  UI_THEMES,
  LOCAL_STORAGE_KEYS,
  API_TYPES,
  INPUT_TYPES,
  USER_DATA_FIELDS,
  SIDE_MENU_ITEMS,
};
